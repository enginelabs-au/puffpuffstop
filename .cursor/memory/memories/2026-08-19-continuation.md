# 2026-08-19 continuation

## Git identity restated

- Owner: never use personal Gmail. Always `Cursor Agent <cursoragent@cursor.com>` via `GIT_AUTHOR_*` / `GIT_COMMITTER_*`. Never change git config.
- Recorded in `/USER.md`, `/memory/MEMORY.md`, `/STATE.md`, and `/memory/memories/git-identity.md`.
- An always-on rule file under `/rules/` was blocked by the fail-closed hook.

## Phase 10 — friendly science onboarding

- Cartoon-friendly tokens, animated organ characters, and a Home / Science / Settings tab bar.
- Organ baselines use a 50-year heavy-use horizon so a few years of vaping stay high. Sources live on Science.
- In-app 16+ notice removed; store listing stays 16+.
- Physical rotary dials with exponential ticks and tap-to-type (999, or 999,999 for device puffs).
- Product catalog with claimed puffs and confirmed nicotine only. All-currency picker. New trigger and quit-window options. Back arrow plus swipe-back.
- Validation: `npm run lint` 0; `npm test` 63/63; `npm run typecheck` 0.
- No commit unless the owner asks.

## Gentler organ ticks

- One log now costs 0.01 percentage points (shown to two decimals). Over-cap extra is 0.004.
- A successful day heals that day's puffs plus a 1.2 bonus, so staying under the cap is clearly visible.
- Home celebrates with bounce, sparkles, a mint/amber glow, copy, and a success haptic when a goal day rolls over or a streak is active.
- Validation: `npm run lint` 0; `npm test` 66/66; `npm run typecheck` 0.

## Duration labels, AU dates, realistic organs

- Duration chips are days/weeks/months/years. Frequency stays singular for “a day”.
- Exact stop date uses DD-MM-YYYY with auto-inserted dashes.
- Home organs are anatomical SVGs (`react-native-svg` + `expo-linear-gradient`) that recolor, stain, breathe, and beat from the user’s score.
